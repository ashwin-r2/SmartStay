// Floating AI chat assistant widget. Full implementation added in a later step;
// this stub keeps the Layout compiling while the rest of the app is scaffolded.
export function AiChatWidget() {
  return null;
}
