# Base URL the frontend uses for API calls. In local dev, Vite's dev server proxies
# "/api" to the backend (see vite.config.ts) so the default below works out of the box.
# In production, point this at your deployed backend's public URL + /api.
VITE_API_BASE_URL=/api
